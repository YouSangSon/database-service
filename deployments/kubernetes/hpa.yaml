apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: database-service-api-hpa
  namespace: database-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: database-service-api
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
      - type: Percent
        value: 100
        periodSeconds: 30
      - type: Pods
        value: 2
        periodSeconds: 30
      selectPolicy: Max
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: database-service-grpc-hpa
  namespace: database-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: database-service-grpc
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
      - type: Percent
        value: 100
        periodSeconds: 30
      - type: Pods
        value: 2
        periodSeconds: 30
      selectPolicy: Max
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: database-service-api-pdb
  namespace: database-service
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: database-service
      component: api
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: database-service-grpc-pdb
  namespace: database-service
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: database-service
      component: grpc
